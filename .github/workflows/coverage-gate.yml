name: Coverage Gate

on:
  pull_request:
    branches: [main]

jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install web dependencies
        working-directory: web
        run: bun install

      - name: Run web tests with coverage
        working-directory: web
        run: bun run test -- --coverage || true

      - name: Install platform dependencies
        working-directory: platform
        run: bun install

      - name: Run platform tests with coverage
        working-directory: platform
        run: bun run test -- --coverage || true

      - name: Enforce 80% coverage on new / changed files
        run: |
          FAIL=0
          THRESHOLD=80

          # ── Helper function ────────────────────────────────────────────
          check_coverage() {
            local PROJECT="$1"   # "web" or "platform"
            # Optional subdirectory filter(s): e.g. "server/" to only check server code
            local SUBDIRS="${2:-server/ src/}"
            local SUMMARY="$PROJECT/coverage/coverage-summary.json"

            if [ ! -f "$SUMMARY" ]; then
              echo "::warning::$PROJECT coverage-summary.json not found — tests may have crashed."
              return
            fi

            # Build the diff paths from project + subdirectories
            local DIFF_PATHS=""
            for sub in $SUBDIRS; do
              DIFF_PATHS="$DIFF_PATHS $PROJECT/$sub"
            done

            # Source files added or modified in this PR (relative to repo root)
            local CHANGED
            CHANGED=$(git diff --name-only --diff-filter=ACM origin/main...HEAD -- $DIFF_PATHS \
              | grep -E '\.(ts|tsx)$' \
              | grep -v '\.test\.\|\.spec\.\|__mocks__\|test-setup' || true)

            if [ -z "$CHANGED" ]; then
              echo "No new/changed source files in $PROJECT — nothing to gate."
              return
            fi

            echo "=== Changed source files in $PROJECT ==="
            echo "$CHANGED"
            echo ""

            for file in $CHANGED; do
              # Build the absolute path key that Istanbul uses in json-summary
              local ABS_KEY
              ABS_KEY="$(pwd)/${file}"

              # jq: look up by absolute path
              local LINES_PCT
              LINES_PCT=$(jq -r --arg f "$ABS_KEY" '.[$f].lines.pct // empty' "$SUMMARY")

              # Strip project prefix for display
              local DISPLAY="${file#$PROJECT/}"

              if [ -z "$LINES_PCT" ]; then
                echo "⚠️  $DISPLAY — no coverage data (not imported by any test)"
                echo "❌ $DISPLAY — treated as 0% (below ${THRESHOLD}%)"
                FAIL=1
                continue
              fi

              # Compare as float
              local BELOW
              BELOW=$(awk "BEGIN { print ($LINES_PCT < $THRESHOLD) ? 1 : 0 }")
              if [ "$BELOW" = "1" ]; then
                echo "❌ $DISPLAY — ${LINES_PCT}% lines covered (threshold: ${THRESHOLD}%)"
                FAIL=1
              else
                echo "✅ $DISPLAY — ${LINES_PCT}% lines covered"
              fi
            done
          }

          # ── Run for both projects ──────────────────────────────────────
          check_coverage "web"
          echo ""
          # Platform: only gate server code (frontend is scaffolding for now)
          check_coverage "platform" "server/"

          echo ""
          if [ "$FAIL" = "1" ]; then
            echo "::error::Coverage gate failed — one or more changed files are below ${THRESHOLD}% line coverage."
            echo "Add tests for the files listed above to bring them above the threshold."
            exit 1
          else
            echo "All changed files meet the ${THRESHOLD}% line coverage threshold."
          fi
